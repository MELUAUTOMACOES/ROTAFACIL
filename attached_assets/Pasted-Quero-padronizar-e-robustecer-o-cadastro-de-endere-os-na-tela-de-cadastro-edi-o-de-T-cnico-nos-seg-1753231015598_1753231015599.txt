Quero padronizar e robustecer o cadastro de endereços na tela de cadastro/edição de Técnico, nos seguintes pontos:

1. Cadastro de Técnico (endereço principal do técnico)
Quero os seguintes campos de endereço, igual ao cadastro de Cliente:

CEP (com busca automática)

Logradouro

Número

Complemento

Bairro

Cidade

Estado (UF)

Regras:

Quando digitar um CEP válido, deve preencher automaticamente Logradouro, Bairro, Cidade e Estado usando a função buscarEnderecoPorCep abaixo.

Permitir editar manualmente qualquer campo após o preenchimento automático.

Validar o CEP com máscara (00000-000) e exibir erro se não encontrar ou inválido.

Organizar todos os campos juntos numa seção chamada "Endereço".

2. Endereço de Início Diário (Opcional)
Quero exatamente a mesma estrutura acima (CEP, Logradouro, Número, Complemento, Bairro, Cidade, Estado), inclusive a busca automática de endereço ao digitar o CEP, igual ao do cadastro do cliente.

Se o usuário preencher o CEP, deve buscar o endereço automaticamente.

Permitir edição manual.

Aplicar as mesmas validações e máscara.

3. Função a ser usada para busca de endereço
Usar exatamente esta função (já existe no projeto):

js
Copiar
Editar
async function buscarEnderecoPorCep(cep: string) {
  const url = `https://viacep.com.br/ws/${cep.replace(/\D/g, '')}/json/`;
  const res = await fetch(url);
  const data = await res.json();
  if (data.erro) throw new Error("CEP não encontrado");
  return data; // {logradouro, bairro, localidade, uf, ...}
}
4. Padrão Visual
Usar os mesmos componentes/validações do cadastro de cliente.

Se possível, copiar o padrão visual dos campos de endereço do cliente.

Seção clara para o endereço principal e para o endereço de início diário (com títulos separados).

5. Comparação para facilitar
ANTES: Só tinha CEP, Logradouro, Número, Complemento.

DEPOIS (QUERO ASSIM):

makefile
Copiar
Editar
CEP:           [     ]
Logradouro:    [     ]
Número:        [     ]
Complemento:   [     ]
Bairro:        [     ]
Cidade:        [     ]
Estado (UF):   [     ]
6. Resumindo
Quero:

 Endereço principal do técnico completo e busca automática de CEP.

 Endereço de início diário (opcional) completo e busca automática de CEP.

 Visual idêntico ao do cliente.

 Permitir edição manual de qualquer campo após preencher pelo CEP.

 Máscara e validação para CEP.

 Usar a função buscarEnderecoPorCep já existente.