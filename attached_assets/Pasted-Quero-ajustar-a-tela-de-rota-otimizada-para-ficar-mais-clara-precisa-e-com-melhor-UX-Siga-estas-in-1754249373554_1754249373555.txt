Quero ajustar a tela de rota otimizada para ficar mais clara, precisa e com melhor UX. Siga estas instruções detalhadas:

1. Cálculo e exibição do ponto de início
O primeiro endereço da sequência de rota atualmente mostra como "ponto de início", mas na verdade o ponto de início deve ser o endereço da empresa ou do técnico/equipe (usado na geocodificação inicial).

Ajuste para exibir no início da sequência um card especial, fora da contagem, com algo como:

java
Copiar
Editar
Início da rota  
(Endereço de partida: ...exibir endereço completo...)  
— —  
O cálculo do tempo e distância entre o endereço de início e o primeiro cliente também deve aparecer nesse primeiro trecho (e contar para o total).

Na sequência:

Para cada parada, mostre o tempo/distância do ponto anterior (incluindo do endereço inicial até o primeiro cliente).

Exemplo visual esperado:
python-repl
Copiar
Editar
Início da rota
Av. Brasil, 123, Centro, Curitiba - 80000-000
— —
1. João Silva
Rua Ribeirão do Pinhal...
2 min   0.8 km
2. Maria Rocha
...
8 min   5.2 km
...
2. Topo da tela
No canto superior direito, abaixo da distância total, exiba o tempo total estimado:

yaml
Copiar
Editar
Distância total: 26.8 km
Tempo total estimado: 40min
Deixe a distância em azul (#2563eb ou a cor usada nos cards) e o tempo em verde (#059669 ou a cor usada nos cards), igual aos badges individuais das paradas.

3. Campos de totais na parte de baixo
Mantenha o mesmo padrão de cor nos badges de total também.

Use sempre a cor azul para distância, verde para tempo, igual aos cards individuais.

4. Aviso de carregamento/otimização
Após o clique no botão "Otimizar Rota", exiba um aviso claro na tela (acima da lista de paradas ou em destaque) dizendo:
"Otimizando rota, aguarde..."
(Use um spinner/ícone de loading se possível, e desabilite o botão enquanto otimiza.)

Remova o aviso assim que a rota for exibida.

5. Exemplo de código
Antes (lógica do primeiro card):
jsx
Copiar
Editar
{optimizedRoute.optimizedOrder.map((appointment, index) => (
  <Card ...>
    // ...dados do cliente...
    <span className="text-blue-600">5.2 km</span>
    <span className="text-green-600">8 min</span>
  </Card>
))}
Depois (adicionar card de início + ajuste do primeiro trecho):
jsx
Copiar
Editar
// Card de início
<Card ...>
  <strong>Início da rota</strong>
  <p>{enderecoInicioCompleto}</p>
  <span>—</span> <span>—</span>
</Card>

// Sequência
{optimizedRoute.optimizedOrder.map((appointment, index) => (
  <Card ...>
    // ...dados do cliente...
    <span className="text-blue-600">{distanciaDoTrecho}</span>
    <span className="text-green-600">{tempoDoTrecho}</span>
  </Card>
))}
Exemplo para o topo:
jsx
Copiar
Editar
<div className="flex flex-col items-end">
  <span className="text-sm text-gray-600">
    Distância total: <span className="text-blue-600 font-semibold">{distanciaTotal}</span>
  </span>
  <span className="text-sm text-gray-600">
    Tempo total estimado: <span className="text-green-600 font-semibold">{tempoTotal}</span>
  </span>
</div>
Exemplo para aviso de otimização:
jsx
Copiar
Editar
{isOtimizing && (
  <div className="flex items-center justify-center text-yellow-700 font-medium my-4">
    <Loader className="animate-spin mr-2" />
    Otimizando rota, aguarde...
  </div>
)}
6. Resumo do que precisa ser feito
Adicione o card de início da rota com tempo/distância até o primeiro ponto.

Corrija o cálculo e exibição dos trechos para considerar do ponto de partida até o primeiro cliente.

Adicione o tempo total estimado abaixo da distância total, em destaque e colorido.

Mantenha as cores dos badges (azul para km, verde para min) nos totais.

Exiba aviso "Otimizando rota, aguarde..." durante o carregamento.

Implemente e mostre exemplos de código conforme acima, adaptando para o contexto e variáveis já usadas no projeto.

Se precisar de dados do endereço inicial ou da matriz OSRM, eles já estão disponíveis via backend.